<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>قائمة الطلبات</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@300;400;500;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary-color: #3498db;
            --secondary-color: #2ecc71;
            --danger-color: #e74c3c;
            --dark-color: #2c3e50;
            --light-color: #ecf0f1;
            --shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Tajawal', sans-serif;
            background-color: #f8f9fa;
            color: #333;
            line-height: 1.6;
            padding: 0;
        }

        .header {
            position: relative;
            height: 250px;
            overflow: hidden;
            margin-bottom: 2rem;
        }

        .header-image {
            width: 100%;
            height: 100%;
            object-fit: cover;
            filter: brightness(0.7);
        }

        .header-content {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            text-align: center;
            color: white;
            z-index: 2;
            width: 80%;
        }

        .header-content h1 {
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 1rem;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
        }

        .header-content p {
            font-size: 1.2rem;
            max-width: 600px;
            margin: 0 auto;
            text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.5);
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 1rem;
        }

        .card {
            background: white;
            border-radius: 10px;
            box-shadow: var(--shadow);
            margin-bottom: 2rem;
            overflow: hidden;
        }

        .card-header {
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            color: white;
            padding: 1rem;
            font-weight: 600;
        }

        .table {
            margin-bottom: 0;
        }

        .table thead th {
            background-color: var(--dark-color);
            color: white;
            border: none;
            padding: 1rem;
            font-weight: 500;
        }

        .table tbody tr {
            transition: all 0.3s ease;
        }

        .table tbody tr:hover {
            background-color: rgba(52, 152, 219, 0.1);
        }

        .table td {
            padding: 1rem;
            vertical-align: middle;
        }

        .btn {
            border-radius: 5px;
            padding: 0.5rem 1rem;
            font-weight: 500;
            transition: all 0.3s ease;
        }

        .btn-danger {
            background-color: var(--danger-color);
            border: none;
        }

        .btn-danger:hover {
            background-color: #c0392b;
            transform: translateY(-2px);
        }

        .list-unstyled li {
            margin-bottom: 0.5rem;
            padding-bottom: 0.5rem;
            border-bottom: 1px solid #eee;
        }

        .list-unstyled li:last-child {
            border-bottom: none;
            margin-bottom: 0;
            padding-bottom: 0;
        }

        .badge {
            padding: 0.5rem 1rem;
            border-radius: 30px;
            font-weight: 500;
        }

        .badge-primary {
            background-color: var(--primary-color);
        }

        .badge-success {
            background-color: var(--secondary-color);
        }

        .empty-state {
            text-align: center;
            padding: 3rem 1rem;
            color: #777;
        }

        .empty-state i {
            font-size: 4rem;
            color: #ddd;
            margin-bottom: 1rem;
        }

        @media (max-width: 768px) {
            .header {
                height: 200px;
            }

            .header-content h1 {
                font-size: 2rem;
            }

            .table-responsive {
                border-radius: 10px;
                overflow: hidden;
            }
        }

        /* Dashboard Styles */
        .dashboard-section {
            margin-bottom: 2rem;
            padding: 1.5rem;
            background: white;
            border-radius: 10px;
            box-shadow: var(--shadow);
        }

        .dashboard-title {
            color: var(--dark-color);
            margin-bottom: 1.5rem;
            font-size: 1.5rem;
            font-weight: 700;
            text-align: center;
        }

        .dashboard-cards {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1.5rem;
        }

        .dashboard-card {
            background: linear-gradient(135deg, #f8f9fa, #e9ecef);
            border-radius: 10px;
            padding: 1.5rem;
            display: flex;
            align-items: center;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .dashboard-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 15px rgba(0, 0, 0, 0.1);
        }

        .card-icon {
            width: 50px;
            height: 50px;
            border-radius: 50%;
            background: var(--primary-color);
            display: flex;
            align-items: center;
            justify-content: center;
            margin-left: 1rem;
        }

        .card-icon i {
            font-size: 1.5rem;
            color: white;
        }

        .card-content {
            flex: 1;
        }

        .card-content h3 {
            font-size: 1rem;
            color: #6c757d;
            margin-bottom: 0.5rem;
        }

        .card-value {
            font-size: 1.5rem;
            font-weight: 700;
            color: var(--dark-color);
            margin: 0;
        }

        @media (max-width: 768px) {
            .dashboard-cards {
                grid-template-columns: repeat(2, 1fr);
            }
        }

        @media (max-width: 480px) {
            .dashboard-cards {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <div class="header">
        <img src="https://images.unsplash.com/photo-1589829545856-d10d557cf95f?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1470&q=80" alt="Commandes" class="header-image">
        <div class="header-content">
            <h1>📦 قائمة الطلبات</h1>
            <p>إدارة وتتبع جميع طلبات العملاء</p>
        </div>
    </div>

    <div class="container">
        <!-- Dashboard Section -->
        <div class="dashboard-section">
            <h2 class="dashboard-title">لوحة المعلومات</h2>
            <div class="dashboard-cards">
                <div class="dashboard-card">
                    <div class="card-icon">
                        <i class="fas fa-shopping-cart"></i>
                    </div>
                    <div class="card-content">
                        <h3>إجمالي الطلبات</h3>
                        <p class="card-value"><%= allcommandes ? allcommandes.length : 0 %></p>
                    </div>
                </div>
                <div class="dashboard-card">
                    <div class="card-icon">
                        <i class="fas fa-money-bill-wave"></i>
                    </div>
                    <div class="card-content">
                        <h3>إجمالي المبيعات</h3>
                        <p class="card-value">
                            <% 
                            if (allcommandes && allcommandes.length > 0) {
                                const totalVentes = allcommandes.reduce((sum, commande) => sum + commande.total, 0);
                            %>
                                <%= totalVentes %> DH
                            <% } else { %>
                                0 DH
                            <% } %>
                        </p>
                    </div>
                </div>
                <div class="dashboard-card">
                    <div class="card-icon">
                        <i class="fas fa-book"></i>
                    </div>
                    <div class="card-content">
                        <h3>إجمالي الكتب المباعة</h3>
                        <p class="card-value">
                            <% 
                            if (allcommandes && allcommandes.length > 0) {
                                const totalLivres = allcommandes.reduce((sum, commande) => {
                                    return sum + commande.livres.reduce((bookSum, item) => bookSum + item.quantite, 0);
                                }, 0);
                            %>
                                <%= totalLivres %>
                            <% } else { %>
                                0
                            <% } %>
                        </p>
                    </div>
                </div>
                <div class="dashboard-card">
                    <div class="card-icon">
                        <i class="fas fa-users"></i>
                    </div>
                    <div class="card-content">
                        <h3>عدد العملاء</h3>
                        <p class="card-value">
                            <% 
                            if (allcommandes && allcommandes.length > 0) {
                                const uniqueClients = new Set(allcommandes.map(commande => 
                                    commande.utilisateur ? commande.utilisateur._id : 'anonymous'
                                ));
                            %>
                                <%= uniqueClients.size %>
                            <% } else { %>
                                0
                            <% } %>
                        </p>
                    </div>
                </div>
            </div>
        </div>

        <div class="card">
            <div class="card-header">
                <i class="fas fa-shopping-cart me-2"></i> تفاصيل الطلبات
            </div>
            <div class="table-responsive">
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th>الإجراءات</th>
                            <th>تاريخ الطلب</th>
                            <th>العنوان</th>
                            <th>المجموع (DH)</th>
                            <th>الكتب المطلوبة</th>
                            <th>البريد الالكتروني</th>
                            <th>اسم المستخدم</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% if (allcommandes && allcommandes.length > 0) { %>
                            <% for (let commande of allcommandes) { %>
                            <tr>
                                <td>
                                    <button class="btn btn-danger btn-sm" onclick="supprimerLivre('<%= commande._id %>')">
                                        <i class="fas fa-trash-alt me-1"></i> حذف
                                    </button>
                                </td>
                                <td><%= new Date(commande.dateCommande).toLocaleString("ar-MA") %></td>
                                <td><%= commande.adresse %></td>
                                <td><span class="badge badge-primary"><%= commande.total %> DH</span></td>
                                <td>
                                    <ul class="list-unstyled">
                                        <% for (let item of commande.livres) { %>
                                            <li><%= item.livre.title %> <span class="badge badge-success">x<%= item.quantite %></span></li>
                                        <% } %>
                                    </ul>
                                </td>
                                <td>
                                    <% if (commande.utilisateur) { %>
                                        <%= commande.utilisateur.email %>
                                    <% } else { %>
                                        <span class="text-muted">مستخدم غير معروف</span>
                                    <% } %>
                                </td>
                                <td>
                                    <% if (commande.utilisateur) { %>
                                        <%= commande.utilisateur.nom %> <%= commande.utilisateur.prenom %>
                                    <% } else { %>
                                        <span class="text-muted">مستخدم غير معروف</span>
                                    <% } %>
                                </td>
                            </tr>
                            <% } %>
                        <% } else { %>
                            <tr>
                                <td colspan="7" class="empty-state">
                                    <i class="fas fa-shopping-cart"></i>
                                    <h3>لا توجد طلبات</h3>
                                    <p>لم يتم العثور على أي طلبات في النظام</p>
                                </td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <script>
        async function supprimerLivre(id) {
            if (confirm("هل أنت متأكد من حذف هذا الطلب؟")) {
                try {
                    const response = await fetch(`/api/commande/${id}`, { method: "DELETE" });
                    if (response.ok) {
                        alert("✅ تم حذف الطلب بنجاح!");
                        location.reload();
                    } else {
                        alert("❌ حدث خطأ أثناء الحذف!");
                    }
                } catch (error) {
                    console.error("Erreur de suppression:", error);
                    alert("⚠️ فشل الاتصال بالخادم!");
                }
            }
        }
    </script>
</body>
</html>
