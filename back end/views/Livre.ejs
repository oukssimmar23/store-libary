<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@300;400;500;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <title>قائمة الكتب</title>
    <style>
        :root {
            --primary-color: #3498db;
            --secondary-color: #2ecc71;
            --warning-color: #f39c12;
            --danger-color: #e74c3c;
            --dark-color: #2c3e50;
            --light-color: #ecf0f1;
            --shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Tajawal', sans-serif;
            background-color: #f8f9fa;
            color: #333;
            line-height: 1.6;
            padding: 0;
        }

        .header {
            position: relative;
            height: 250px;
            overflow: hidden;
            margin-bottom: 2rem;
        }

        .header-image {
            width: 100%;
            height: 100%;
            object-fit: cover;
            filter: brightness(0.7);
        }

        .header-content {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            text-align: center;
            color: white;
            z-index: 2;
            width: 80%;
        }

        .header-content h1 {
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 1rem;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
        }

        .header-content p {
            font-size: 1.2rem;
            max-width: 600px;
            margin: 0 auto;
            text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.5);
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 1rem;
        }

        .card {
            background: white;
            border-radius: 10px;
            box-shadow: var(--shadow);
            margin-bottom: 2rem;
            overflow: hidden;
        }

        .card-header {
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            color: white;
            padding: 1rem;
            font-weight: 600;
        }

        .table {
            margin-bottom: 0;
        }

        .table thead th {
            background-color: var(--dark-color);
            color: white;
            border: none;
            padding: 1rem;
            font-weight: 500;
        }

        .table tbody tr {
            transition: all 0.3s ease;
        }

        .table tbody tr:hover {
            background-color: rgba(52, 152, 219, 0.1);
        }

        .table td {
            padding: 1rem;
            vertical-align: middle;
        }

        .btn {
            border-radius: 5px;
            padding: 0.5rem 1rem;
            font-weight: 500;
            transition: all 0.3s ease;
            margin: 0 0.25rem;
        }

        .btn-danger {
            background-color: var(--danger-color);
            border: none;
        }

        .btn-warning {
            background-color: var(--warning-color);
            border: none;
            color: white;
        }

        .btn-success {
            background-color: var(--secondary-color);
            border: none;
        }

        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }

        .badge {
            padding: 0.5rem 1rem;
            border-radius: 30px;
            font-weight: 500;
        }

        .badge-primary {
            background-color: var(--primary-color);
        }

        .badge-warning {
            background-color: var(--warning-color);
        }

        .action-buttons {
            display: flex;
            justify-content: center;
        }

        .add-books-section {
            text-align: center;
            margin: 2rem 0;
            padding: 2rem;
            background: white;
            border-radius: 10px;
            box-shadow: var(--shadow);
        }

        .add-books-section h2 {
            margin-bottom: 1.5rem;
            color: var(--dark-color);
        }

        .book-type-buttons {
            display: flex;
            justify-content: center;
            gap: 1rem;
            margin-bottom: 2rem;
        }

        .book-type-btn {
            padding: 1rem 2rem;
            border-radius: 10px;
            font-weight: 600;
            transition: all 0.3s ease;
            border: none;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .book-type-btn i {
            font-size: 1.5rem;
        }

        .book-type-btn:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 15px rgba(0, 0, 0, 0.1);
        }

        .arabic-btn {
            background: linear-gradient(135deg, #8e44ad, #9b59b6);
            color: white;
        }

        .french-btn {
            background: linear-gradient(135deg, #3498db, #2980b9);
            color: white;
        }

        .empty-state {
            text-align: center;
            padding: 3rem 1rem;
            color: #777;
        }

        .empty-state i {
            font-size: 4rem;
            color: #ddd;
            margin-bottom: 1rem;
        }

        @media (max-width: 768px) {
            .header {
                height: 200px;
            }

            .header-content h1 {
                font-size: 2rem;
            }

            .table-responsive {
                border-radius: 10px;
                overflow: hidden;
            }

            .book-type-buttons {
                flex-direction: column;
            }
        }

        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0,0,0,0.5);
            z-index: 1000;
        }

        .modal-content {
            background-color: #fff;
            margin: 5% auto;
            padding: 2rem;
            border-radius: 12px;
            width: 90%;
            max-width: 600px;
            direction: rtl;
        }

        .close {
            float: left;
            font-size: 28px;
            font-weight: bold;
            cursor: pointer;
        }

        .form-group {
            margin-bottom: 1.5rem;
        }

        .form-group label {
            display: block;
            margin-bottom: 0.5rem;
            font-weight: bold;
        }

        .form-group input,
        .form-group textarea {
            width: 100%;
            padding: 0.75rem;
            border: 2px solid #e9ecef;
            border-radius: 8px;
            font-size: 1rem;
        }

        .form-group textarea {
            height: 100px;
            resize: vertical;
        }

        .form-buttons {
            display: flex;
            gap: 1rem;
            justify-content: flex-end;
            margin-top: 2rem;
        }

        .submit-btn,
        .cancel-btn {
            padding: 0.75rem 1.5rem;
            border-radius: 50px;
            border: none;
            font-size: 1rem;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .submit-btn {
            background-color: #2A5C82;
            color: white;
        }

        .cancel-btn {
            background-color: #e9ecef;
            color: #6c757d;
        }

        .submit-btn:hover {
            background-color: #1a3a5c;
        }

        .cancel-btn:hover {
            background-color: #dee2e6;
        }

        /* Dashboard Styles */
        .dashboard-section {
            margin-bottom: 2rem;
            padding: 1.5rem;
            background: white;
            border-radius: 10px;
            box-shadow: var(--shadow);
        }

        .dashboard-title {
            color: var(--dark-color);
            margin-bottom: 1.5rem;
            font-size: 1.5rem;
            font-weight: 700;
            text-align: center;
        }

        .dashboard-cards {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1.5rem;
        }

        .dashboard-card {
            background: linear-gradient(135deg, #f8f9fa, #e9ecef);
            border-radius: 10px;
            padding: 1.5rem;
            display: flex;
            align-items: center;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .dashboard-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 15px rgba(0, 0, 0, 0.1);
        }

        .card-icon {
            width: 50px;
            height: 50px;
            border-radius: 50%;
            background: var(--primary-color);
            display: flex;
            align-items: center;
            justify-content: center;
            margin-left: 1rem;
        }

        .card-icon i {
            font-size: 1.5rem;
            color: white;
        }

        .card-content {
            flex: 1;
        }

        .card-content h3 {
            font-size: 1rem;
            color: #6c757d;
            margin-bottom: 0.5rem;
        }

        .card-value {
            font-size: 1.5rem;
            font-weight: 700;
            color: var(--dark-color);
            margin: 0;
        }

        @media (max-width: 768px) {
            .dashboard-cards {
                grid-template-columns: repeat(2, 1fr);
            }
        }

        @media (max-width: 480px) {
            .dashboard-cards {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <div class="header">
        <img src="https://images.unsplash.com/photo-1524995997946-a1c2e315a42f?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1470&q=80" alt="Livres" class="header-image">
        <div class="header-content">
            <h1>📚 قائمة الكتب</h1>
            <p>إدارة وتتبع جميع الكتب في المكتبة</p>
        </div>
    </div>

    <div class="container">
        <!-- Dashboard Section -->
        <div class="dashboard-section">
            <h2 class="dashboard-title">لوحة المعلومات</h2>
            <div class="dashboard-cards">
                <div class="dashboard-card">
                    <div class="card-icon">
                        <i class="fas fa-book"></i>
                    </div>
                    <div class="card-content">
                        <h3>إجمالي الكتب</h3>
                        <p class="card-value"><%= alllivres ? alllivres.length : 0 %></p>
                    </div>
                </div>
                <div class="dashboard-card">
                    <div class="card-icon">
                        <i class="fas fa-star"></i>
                    </div>
                    <div class="card-content">
                        <h3>متوسط التقييم</h3>
                        <p class="card-value">
                            <% 
                            if (alllivres && alllivres.length > 0) {
                                const totalEval = alllivres.reduce((sum, livre) => sum + livre.evaluation, 0);
                                const avgEval = (totalEval / alllivres.length).toFixed(1);
                            %>
                                <%= avgEval %>/10
                            <% } else { %>
                                0/10
                            <% } %>
                        </p>
                    </div>
                </div>
                <div class="dashboard-card">
                    <div class="card-icon">
                        <i class="fas fa-money-bill-wave"></i>
                    </div>
                    <div class="card-content">
                        <h3>متوسط السعر</h3>
                        <p class="card-value">
                            <% 
                            if (alllivres && alllivres.length > 0) {
                                const totalPrix = alllivres.reduce((sum, livre) => sum + livre.prix, 0);
                                const avgPrix = (totalPrix / alllivres.length).toFixed(1);
                            %>
                                <%= avgPrix %> DH
                            <% } else { %>
                                0 DH
                            <% } %>
                        </p>
                    </div>
                </div>
                <div class="dashboard-card">
                    <div class="card-icon">
                        <i class="fas fa-layer-group"></i>
                    </div>
                    <div class="card-content">
                        <h3>عدد الفئات</h3>
                        <p class="card-value">
                            <% 
                            if (alllivres && alllivres.length > 0) {
                                const categories = new Set(alllivres.map(livre => livre.categorie));
                            %>
                                <%= categories.size %>
                            <% } else { %>
                                0
                            <% } %>
                        </p>
                    </div>
                </div>
            </div>
        </div>

        <div class="add-books-section">
            <h2>إضافة كتب جديدة</h2>
            <div class="book-type-buttons">
                <button class="book-type-btn arabic-btn" onclick="ajouterLivreArabe()">
                    <i class="fas fa-book-open"></i> إضافة كتاب عربي
                </button>
                <button class="book-type-btn french-btn" onclick="ajouterLivre()">
                    <i class="fas fa-book"></i> إضافة كتاب فرنسي
                </button>
            </div>
        </div>

        <div class="card">
            <div class="card-header">
                <i class="fas fa-books me-2"></i> تفاصيل الكتب
            </div>
            <div class="table-responsive">
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th>الإجراءات</th>
                            <th>السعر (DH)</th>
                            <th>التقييم</th>
                            <th>العدد</th>
                            <th>المؤلف</th>
                            <th>العنوان</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% if (alllivres && alllivres.length > 0) { %>
                            <% for(let livre of alllivres) { %>
                            <tr>
                                <td class="action-buttons">
                                    <button class="btn btn-danger btn-sm" onclick="supprimerLivre('<%= livre._id %>')">
                                        <i class="fas fa-trash-alt"></i>
                                    </button>
                                    <button class="btn btn-warning btn-sm" onclick="modifierLivre('<%= livre._id %>')">
                                        <i class="fas fa-edit"></i>
                                    </button>
                                </td>
                                <td><span class="badge badge-primary"><%= livre.prix %> DH</span></td>
                                <td><span class="badge badge-warning"><%= livre.evaluation %>/10 ⭐</span></td>
                                <td><%= livre.count %></td>
                                <td><%= livre.auteur %></td>
                                <td><%= livre.title %></td>
                            </tr>
                            <% } %>
                        <% } else { %>
                            <tr>
                                <td colspan="6" class="empty-state">
                                    <i class="fas fa-books"></i>
                                    <h3>لا توجد كتب</h3>
                                    <p>لم يتم العثور على أي كتب في النظام</p>
                                </td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <!-- Modal Form -->
    <div id="bookModal" class="modal">
        <div class="modal-content">
            <span class="close">&times;</span>
            <h2 id="modalTitle">إضافة كتاب جديد</h2>
            <form id="bookForm">
                <div class="form-group">
                    <label for="bookId">🔑 المعرف:</label>
                    <input type="number" id="bookId" required>
                </div>
                <div class="form-group">
                    <label for="title">📖 العنوان:</label>
                    <input type="text" id="title" required>
                </div>
                <div class="form-group">
                    <label for="auteur">✍️ المؤلف:</label>
                    <input type="text" id="auteur" required>
                </div>
                <div class="form-group">
                    <label for="categorie">📚 النوع:</label>
                    <input type="text" id="categorie" required>
                </div>
                <div class="form-group">
                    <label for="evaluation">⭐ التقييم (من 10):</label>
                    <input type="number" id="evaluation" min="0" max="10" required>
                </div>
                <div class="form-group">
                    <label for="prix">💰 السعر (DH):</label>
                    <input type="number" id="prix" required>
                </div>
                <div class="form-group">
                    <label for="description">📝 الوصف:</label>
                    <textarea id="description" required></textarea>
                </div>
                <div class="form-group">
                    <label for="count">🔢 عدد النسخ:</label>
                    <input type="number" id="count" required>
                </div>
                <div class="form-group">
                    <label for="image">🖼 رابط الصورة:</label>
                    <input type="url" id="image" required>
                </div>
                <div class="form-buttons">
                    <button type="submit" class="submit-btn">حفظ</button>
                    <button type="button" class="cancel-btn">إلغاء</button>
                </div>
            </form>
        </div>
    </div>

    <script>
        const modal = document.getElementById('bookModal');
        const form = document.getElementById('bookForm');
        const closeBtn = document.querySelector('.close');
        const cancelBtn = document.querySelector('.cancel-btn');
        let currentAction = 'add';

        // Ouvrir le modal
        function openModal(action, bookData = null) {
            currentAction = action;
            const modalTitle = document.getElementById('modalTitle');
            modalTitle.textContent = action === 'add' ? 'إضافة كتاب جديد' : 'تعديل الكتاب';
            
            if (bookData) {
                document.getElementById('bookId').value = bookData.id;
                document.getElementById('title').value = bookData.title;
                document.getElementById('auteur').value = bookData.auteur;
                document.getElementById('categorie').value = bookData.categorie;
                document.getElementById('evaluation').value = bookData.evaluation;
                document.getElementById('prix').value = bookData.prix;
                document.getElementById('description').value = bookData.description;
                document.getElementById('count').value = bookData.count;
                document.getElementById('image').value = bookData.image;
            } else {
                form.reset();
            }
            
            modal.style.display = 'block';
        }

        // Fermer le modal
        function closeModal() {
            modal.style.display = 'none';
            form.reset();
        }

        // Gestionnaires d'événements
        closeBtn.onclick = closeModal;
        cancelBtn.onclick = closeModal;
        window.onclick = (e) => {
            if (e.target === modal) closeModal();
        }

        // Soumission du formulaire
        form.onsubmit = async (e) => {
            e.preventDefault();
            
            const formData = {
                id: Number(document.getElementById('bookId').value),
                title: document.getElementById('title').value,
                auteur: document.getElementById('auteur').value,
                categorie: document.getElementById('categorie').value,
                evaluation: Number(document.getElementById('evaluation').value),
                prix: Number(document.getElementById('prix').value),
                description: document.getElementById('description').value,
                count: Number(document.getElementById('count').value),
                image: document.getElementById('image').value
            };

            try {
                const url = currentAction === 'add' ? '/api/livres' : `/api/livre/${document.getElementById('bookId').value}`;
                const method = currentAction === 'add' ? 'POST' : 'PUT';
                
                const response = await fetch(url, {
                    method: method,
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(formData)
                });

                if (response.ok) {
                    alert(`✅ تم ${currentAction === 'add' ? 'إضافة' : 'تعديل'} الكتاب بنجاح!`);
                    location.reload();
                } else {
                    alert('❌ حدث خطأ أثناء العملية!');
                }
            } catch (error) {
                console.error('Erreur:', error);
                alert('⚠️ فشل الاتصال بالخادم!');
            }
        };

        // Fonctions pour les boutons d'action
        function ajouterLivre() {
            openModal('add');
        }

        function modifierLivre(id) {
            // Récupérer les données du livre et ouvrir le modal
            const bookData = {
                id: id,
                // Ajouter les autres données du livre ici
            };
            openModal('edit', bookData);
        }

        async function supprimerLivre(id) {
            if (confirm('هل أنت متأكد من حذف هذا الكتاب؟')) {
                try {
                    const response = await fetch(`/api/livre/${id}`, { method: 'DELETE' });
                    if (response.ok) {
                        alert('✅ تم حذف الكتاب بنجاح!');
                        location.reload();
                    } else {
                        alert('❌ حدث خطأ أثناء الحذف!');
                    }
                } catch (error) {
                    console.error('Erreur de suppression:', error);
                    alert('⚠️ فشل الاتصال بالخادم!');
                }
            }
        }

        // إضافة كتاب عربي
        function ajouterLivreArabe() {
            openModal('add');
        }
    </script>
</body>
</html>